## EXERCISE 2.8

### Commands
```markdown
// Create file docker-compose.yml
$ touch docker-compose.yml

// Create file nginx.conf
$ touch nginx.conf

// Running Docker Compose
$ docker-compose up
```
